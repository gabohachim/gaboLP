name: gabolp
description: GaBoLP - Colección de vinilos
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Base de datos
  sqflite: ^2.3.3
  path: ^1.9.0

  # Google / Nube
  google_sign_in: ^6.2.1
  googleapis: ^13.2.0
  http: ^1.2.2

  # Respaldo / preferencias
  shared_preferences: ^2.2.3

  cupertino_icons: ^1.0.6

dev_dependencies:
 dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.4.0


flutter:
  uses-material-design: true
